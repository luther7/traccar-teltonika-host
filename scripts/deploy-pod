#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

source scripts/utils.bash
echo "--> Starting deploy-pod"
_scp podman/traccar.yaml /storage/traccar.yaml
_ssh systemctl --user restart traccar
echo "--> Completed deploy-pod"
